<Window x:Class="Radio.PopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Radio"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#CC000000"
        Title="Add New Radio" WindowStartupLocation="CenterScreen">
    <Grid>
        <StackPanel Margin="15">
            <TextBlock Text="Stream Url:" Margin="0,0,0,5" Foreground="#FFFFFF" />
            <TextBox x:Name="Url" Margin="0,0,0,10" Background="#80FFFFFF"/>

            <TextBlock Text="Radio Name:" Margin="0,0,0,5"  Foreground="#FFFFFF"/>
            <TextBox x:Name="RadioName" Margin="0,0,0,10" Background="#80FFFFFF"/>

            <TextBlock Text="Description:" Margin="0,0,0,5" Foreground="#FFFFFF"/>
            <TextBox x:Name="Description" Margin="0,0,0,10" Background="#80FFFFFF"/>

            <DockPanel>
                <Button Content="Close" Width="80" HorizontalAlignment="Left" Padding="5" Background="#FFFFFF" Click="CloseButton_Click"/>
                <Button Content="Save" Width="80" HorizontalAlignment="Right" Padding="5" Background="#FFFFFF" Click="OkButton_Click"/>
            </DockPanel>
        </StackPanel>
    </Grid>
</Window>
